# Task 020: 障壁・肩代わり

## 目的

HPより先に消費される複数レイヤー障壁と、最終ダメージを味方へ分配する肩代わりを、T016の効果処理順へ沿う決定論的なダメージ解決として実装する。

## 背景・参照仕様

- `docs/07_BATTLE_EFFECTS.md`
- `docs/17_IMPLEMENTATION_ROADMAP.md`
- 前提タスク: T016～T019

## 実装範囲

- ユニットごとの複数障壁レイヤー
- 障壁合計値の取得とUI表示
- 最終ダメージを対象分・肩代わり分へ分配
- 対象側・肩代わり側それぞれの障壁消費
- 肩代わり側の防御値では再計算しない
- 肩代わりから別の肩代わりへは連鎖させない
- 命中時とHPダメージ時の反応シグナルを区別
- 障壁吸収・破壊・肩代わり分配のイベントログ
- ヘッドレス戦闘への統合

## 対象外

- 障壁付与スキルのコンテンツ量産
- 複数の肩代わり元を同時に重ねる構成
- 肩代わり率の動的変更
- 被弾反応の完成版コンテンツ
- 障壁専用アニメーション

## 守るべき不変条件

- 障壁はHPより先に消費する
- 複数障壁はapplicationSequence、barrierLayerIdの順で安定消費する
- 最終ダメージを分配してから各受け手の障壁へ適用する
- 肩代わり側のDefenseではダメージを再計算しない
- 肩代わりダメージは再度肩代わりしない
- 障壁が全吸収しても対象への命中シグナルは成立する
- HPが減らなければHPダメージシグナルは成立しない
- すべての状態更新とログ順序は乱数を使わない

## 完了条件

- [x] 複数障壁レイヤーを追加・保持・消費できる
- [x] 障壁合計を取得してUIへ表示できる
- [x] 全吸収・部分吸収・複数レイヤー貫通が動作する
- [x] 最終ダメージの肩代わり分配が動作する
- [x] 対象側と肩代わり側の障壁が独立して機能する
- [x] 肩代わりの連鎖が一段階で停止する
- [x] 命中時とHPダメージ時の反応シグナルを区別できる
- [x] 障壁・肩代わり付きヘッドレス戦闘が決定論的に完走する

## 自動テスト

- [x] 障壁の全吸収・部分吸収・レイヤー順序・破壊
- [x] 障壁合計と不変性
- [x] 肩代わり分配と丸め
- [x] 肩代わり側障壁
- [x] Defense再計算なし
- [x] 連鎖一段停止
- [x] 命中時 / HPダメージ時シグナル
- [x] イベントsequenceと再生状態
- [x] 既存テスト全件
- [x] `pnpm demo:headless`
- [x] `pnpm lint`
- [x] `pnpm build`

## データ・セーブ互換性

- BattleUnitStateへbarriersを追加するが、未指定時は空配列とする。
- HeadlessBattleDefinitionのguardSharesは任意項目とする。
- 永続セーブは未実装のため既存セーブ互換性への影響はない。

## ドキュメント更新

- [x] `tasks/STATUS.md` を更新する
- [x] `README.md` の現在地を更新する

## 完了結果

- 障壁は付与順の安定したレイヤーとして保持し、HPより先に消費する。
- 肩代わりは最終ダメージを半切り上げで分配し、各受け手の障壁へ独立して適用する。
- 障壁で全吸収した場合も命中シグナルを残し、HPダメージシグナルは発生させない。
- 障壁吸収・破壊・肩代わり分配を連続sequenceのイベントログへ記録し、再生UIで障壁合計を表示する。
- GitHub Actions CI run 81でtest、headless demo、lint、buildが成功した。

## 次のタスク

- T021 到達方式・範囲マスク・強制移動
