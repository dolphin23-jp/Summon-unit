# Task 017: 属性・バフデバフ・状態耐性

## 目的

7属性の相性、主要戦闘ステータスのバフ・デバフ、タグ式状態耐性を決定論的な戦闘計算へ追加する。

## 背景・参照仕様

- `docs/05_MONSTER_SYSTEM.md`
- `docs/07_BATTLE_EFFECTS.md`
- `docs/15_BALANCE_GUIDELINES.md`
- `docs/16_DATA_SCHEMAS.md`
- `docs/17_IMPLEMENTATION_ROADMAP.md`
- 前提タスク: T016

## 実装範囲

- 無・火・水・風・地・光・闇の属性IDと相性表
- 有利1.25、不利0.80、等倍1.00の整数倍率
- 属性倍率を単体ダメージ計算へ統合
- Attack / Defense / Speed / Action Cost / Healing Receivedのバフ・デバフ
- `ActiveEffectState`を入力とするステータス倍率集計と上限処理
- Speed / Action Cost補正を技と通常移動の再スケジュールへ統合
- 状態耐性タグと無効 / 50 / 75 / 100 / 125の5段階
- 状態耐性倍率の決定論的解決API

## 対象外

- 毒・火傷など個別状態異常の付与・継続処理
- 回復技そのものの実装
- 属性・状態アイコンのUI表示
- 確率命中・確率耐性
- AI評価スコアへの属性・効果補正の統合（T025で評価基盤ごと実装）

## 変更可能な領域

- `src/content/`
- `src/battle/`
- 必要最小限のデモデータとテスト
- `tasks/STATUS.md`
- `README.md`

## 守るべき不変条件

- すべての倍率は整数permilleで保持する
- 属性相性とステータス上限は一つの定義へ集約する
- 同じ効果集合から同じ補正値を得る
- 状態耐性は乱数を使わずタグと段階だけで解決する
- 属性未指定の既存技は移行互換のため無属性として扱う
- 既存の無効果戦闘結果を不必要に変えない

## 完了条件

- [x] 7属性と相性倍率をデータ化できる
- [x] 属性倍率が単体ダメージへ反映される
- [x] 5種のステータス補正と上下限が動作する
- [x] SpeedとAction Cost補正が技・通常移動の再スケジュールへ反映される
- [x] 状態耐性タグから5段階を解決できる

## 自動テスト

- [x] 四属性循環、光闇相互有利、無属性等倍
- [x] 属性を含むダメージの最終丸め
- [x] バフ・デバフの加算、スタック、上下限
- [x] Speed / Action Cost補正後の技・通常移動の行動時刻
- [x] 状態耐性の既定値、各段階、競合タグ拒否
- [x] 既存テスト全件
- [x] `pnpm lint`
- [x] `pnpm demo:headless`
- [x] `pnpm build`

## 手動確認

1. `pnpm test` を実行する
2. `pnpm demo:headless` を実行し、戦闘が決定論的に完走することを確認する
3. `pnpm lint` を実行する
4. `pnpm build` を実行する

GitHub Actions CI run 48で全工程成功。

## データ・セーブ互換性

- 戦闘マスターへ属性定義を追加する。永続セーブは未実装のため既存セーブ互換性への影響はない。
- 属性未指定の既存技は無属性として扱う。
- 既存の `attribute.air` は移行期間のみ `attribute.wind` の別名として正規化し、標準デモは正規IDへ移行済み。

## ドキュメント更新

- [x] `tasks/STATUS.md` を更新する
- [x] `README.md` の現在地を更新する

## 完了報告に含めること

- 属性相性表と倍率
- ステータス補正の上限
- 状態耐性タグ規約
- テスト結果
- 未完了・残課題

## 次のタスク

- T018 状態異常一式
