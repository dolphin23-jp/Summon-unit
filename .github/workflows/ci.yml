name: CI

on:
  pull_request:

permissions:
  contents: write

jobs:
  build-diagnostics:
    runs-on: ubuntu-latest
    timeout-minutes: 8

    steps:
      - name: Checkout pull request branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.13.1 --activate

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Capture build diagnostics
        run: |
          set +e
          set -o pipefail
          pnpm build 2>&1 | tee /tmp/t052-build.log
          status=${PIPESTATUS[0]}
          if [ "$status" -ne 0 ]; then
            cp /tmp/t052-build.log tasks/T052_BUILD_DIAGNOSTICS.txt
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add tasks/T052_BUILD_DIAGNOSTICS.txt
            git commit -m "Record T052 build diagnostics"
            git push origin HEAD:${{ github.head_ref }}
          fi
          exit "$status"
