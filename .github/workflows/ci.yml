name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@10.13.1 --activate

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Content validation
        run: pnpm validate:content

      - name: Test
        run: pnpm test

      - name: Headless demo
        run: pnpm demo:headless > /tmp/headless-battle-demo.json

      - name: AI regression benchmark
        run: pnpm benchmark:ai

      - name: Run vertical slice balance benchmark
        id: balance_benchmark
        continue-on-error: true
        run: pnpm benchmark:balance > /tmp/vertical-slice-balance.json 2> /tmp/vertical-slice-balance.err

      - name: Upload vertical slice balance output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vertical-slice-balance-output
          path: |
            /tmp/vertical-slice-balance.json
            /tmp/vertical-slice-balance.err

      - name: Stop on balance benchmark failure
        if: steps.balance_benchmark.outcome == 'failure'
        run: exit 1

      - name: Lint
        run: pnpm lint

      - name: Build
        run: pnpm build
